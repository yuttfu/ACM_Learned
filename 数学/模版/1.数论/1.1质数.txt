#include<iostream>
using namespace std;

bool is_prime(int n) {
    if (n < 2) return false;
    for (int i = 2;i < n;i++)  *******
        if (n % i == 0)
            return false;
    return true;
}//此方法为o(n),

bool is_prime(int n){
    if(i < 2) return false;
    for(int i = 2;i <= n/i;i++){
        if(n % i == 0){
            return false;
        }
    }
    return true;
}//此方法为o(sqrt(n)),

int main(){
    int n;
    scanf("%d",&n);
    printf("%d\n",is_prime(n));
    return 0;
}